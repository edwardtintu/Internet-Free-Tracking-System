# Environment variables for TARP Project

# Google Maps API Key (optional - leave empty to use OpenStreetMap)
# To use Google Maps, obtain an API key from Google Cloud Console and restrict it appropriately
GOOGLE_MAPS_API_KEY=

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000,http://192.168.1.3:5000

# Serial communication (for direct connection to receiver, if not using WiFi)
SERIAL_ENABLED=False
SERIAL_PORT=COM5
SERIAL_BAUD=115200